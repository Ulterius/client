//since we only want jasmine here, in this file.
// /// <reference path="../typings/jasmine/jasmine.d.ts" />

/*

import "../src/alt.ts"
import {appActions} from "../src/action"
import {appStore} from "../src/store"
import setIntervals from "../src/interval"

describe("App store", () => {
    
    it("can log in and log out", () => {
        appActions.login(true)
        expect(appStore.getState().auth.loggedIn).toBe(true)
        appActions.login(false)
        expect(appStore.getState().auth.loggedIn).toBe(false)
    })
    
    it("can have keys set", () => {
        appActions.setKey("ayy", "lmao")
        let {key, iv} = appStore.getState().crypto
        expect(key).toBe("ayy")
        expect(iv).toBe("lmao")
    })
    
})

test? what is this "test" you speak of?

*/